---
paths:
  - "src/components/**/*.tsx"
  - "src/features/**/components/**/*.tsx"
  - "components/**/*.tsx"
---

# Component Conventions

- Invoke the `creating-component` skill before creating new components.
- Map Figma design tokens by NAME, not hex value (hex may differ slightly).

For full details, see `.docs/conventions/component-workflow.md` and `.docs/conventions/codebase-structure.md`.
