# PR Review Queue — 2026-05-01

## wanderu/canopy #463 — ci: adopt shared ship-it workflow, drop release-please

**URL:** https://github.com/wanderu/canopy/pull/463
**Branch:** feat/ship-it-adoption → main
**Head SHA:** 6b168f034a856ebca19d0077b577dd2748b6f265
**Classification:** NEEDS_REVIEW
**Reviewed:** 2026-05-01
**Existing review:** scottpcipriano — APPROVED

### Summary

6 files changed (+47/-172). Replaces release-please-gated sequential CI pipeline (deploy-prod.yaml + release-deploy.yaml + release-please config) with `wanderu/actions/.github/workflows/ship-it.yaml@latest` shared reusable workflow. Every merge to main now deploys dev+pilot in parallel, gates prod on smoke tests, with a circuit breaker (`SHIP_IT_HALTED`). Post-deploy tests on dev already passing per CI comments.

### BLOCKING: None

### SUGGESTIONS: None

### DISPUTED (weakened by independent challengers)

**D1 — `prod` standalone dispatch option bypasses dev/pilot**
- File: `.github/workflows/ship-it.yaml`, lines 19-23
- The `environments` choice includes `prod` as a standalone option, allowing a single-click prod-only deploy with no staging validation.
- Challenger: WEAKENED — `eks-deploy.yaml` already has an ungated `prod` dispatch option (pre-existing). No `environment:` protection rules exist anywhere in this repo. Not a regression introduced by this PR.

**D2 — `secrets: inherit` differs from repo's explicit-secrets pattern**
- File: `.github/workflows/ship-it.yaml`, line 33
- Every other reusable-workflow caller enumerates specific secrets. This PR uses `secrets: inherit`.
- Challenger: WEAKENED — `secrets: inherit` is a legitimate pattern for shared reusable workflows. PR claims secrets are validated up-front in the called workflow. Inconsistency is real but likely intentional.

**D3 — `runs-on/action@v2` floating major-version tag**
- File: `.github/workflows/reset-ship-it.yaml`, line 10
- Challenger: WEAKENED — SHA-pinning is not a repo-wide convention. Only 1 of 13 workflow files pins to SHA; all others use floating tags. `runs-on/action` also needs to match runner version.

**D4 — No approval gate on circuit-breaker reset**
- File: `.github/workflows/reset-ship-it.yaml`
- Challenger: WEAKENED — rollback, restart, and eks-deploy workflows are equally unprotected. Repo-wide environment protection rules would be the right fix.

### UNCERTAIN

**U1 — `environment_specific_build: true` vs "Build once / push to all 3 ECRs"**
- File: `.github/workflows/ship-it.yaml`, line 30
- PR description says "Build once / push to all 3 ECRs" but `environment_specific_build: true` implies separate builds per env. Cannot verify without access to `wanderu/actions` source.
- Recommend: Ask author whether this is 3 separate builds or 1 shared image.

### NITS

**N1** — `.github/workflows/ship-it.yaml` line 16: `description: 'Environments to deploy to'` doesn't mention that `prod` alone skips dev/pilot staging.

**N2** — `.github/workflows/ship-it.yaml` line 17: `default: 'dev'` applies only to workflow_dispatch; push path effective default is `dev,pilot,prod` via fallback on line 29 — misleading if reading inputs block in isolation.

### Final Assessment

**Review action: COMMENT** (no blocking findings)
**Blocking:** 0 | **Suggestions:** 0 confirmed, 4 disputed | **Nits:** 2

All disputed findings stem from pre-existing repo patterns rather than regressions. CI green, dev post-deploy tests passing, scott approved.
