# Review Queue — 2026-05-18

## wanderu/ui-react#970 — fix(seo): guard ex.responseBody?.error.status in c2c fetch catch block

**Reviewed:** 2026-05-18  
**Classification:** NEEDS_REVIEW (first review)  
**Review action:** COMMENT  
**Blocking:** none  
**Nits:** 1

### Summary

One-character production fix (`?.` added to `ex.responseBody` access in `fetchCityToCityData` catch block). Fixes 102 errors / 62 sessions over 7 days per Datadog RUM. Fix is correct and well-targeted. Tests are solid and follow the repo's canonical action test pattern.

### Confirmed Findings

**[.docs/plans/rum-citytocity-departure-error/2026-05-15-guard-responsebody.md:13] — nit**  
The "Related Spec" field contains an absolute local machine path: `/Users/scipriano/.dispatcher/worktrees/f8243af0-1f18-4143-98d3-ed082d343848/.docs/specs/rum-citytocity-departure-error/spec.md`  
Should be the repo-relative path: `.docs/specs/rum-citytocity-departure-error/spec.md`

### Refuted Findings (dropped)

- **Partial optional chain concern** — service layer (`clientApiService.ts`, `serverApiService.ts`) guarantees `responseBody.error` is always truthy whenever `responseBody` is set. Single `?.` guard on `responseBody` is sufficient.
- **HTTP-error test missing TypeError assertion** — unguarded `await` in `beforeEach` is actually stricter; any thrown error fails the `beforeEach` immediately, not silently.

### Disputed Findings (held, not posted)

- **Shared store across test describe blocks** — `_endWork` replaces state per ID from `state.default` baseline so no actual bleed occurs; mirrors established `collectEmailAddress` sibling pattern.
- **Knowledge doc missing logger.warn** — doc is scoped to error-state contract, not all side effects; omission is an operational gap but doesn't violate stated scope.
- **`success: false` vs `success: !error` in doc** — equivalent in all real code paths; `error` is always a non-null object at the `endWork` call site described.

---

*Generated by automated PR review — 2026-05-18*

---

## wanderu/ui-react#960 — fix: c2c page crash on missing locationData (departure/arrival)

**Reviewed:** 2026-05-18  
**Classification:** NEEDS_REVIEW (first review)  
**Review action:** COMMENT  
**Blocking:** none  
**Suggestions:** 0  
**Nits:** 0

### Summary

Targeted, well-scoped crash fix. Guards three call sites in `wanderu-app-seo` against `TypeError` crashes when WAPI returns `locationData` with a missing `.departure` or `.arrival` (canonical reproducer: Yosemite → Santa Barbara train route). The fix centralizes the guard in `getPSearchParams` (the upstream producer), then adds early-exit + `logger.warn` in the two downstream callers that would have crashed on the `undefined` result. A UTC-fallback in `fetchTimeTableData` handles the adjacent missing-timezone case. New spec, plan, and specs README are clean. CLAUDE.md update matches the new `.docs/` convention.

Tests are solid for the `getPSearchParams` utility layer (4 new cases covering all guard branches per spec §6). The `fetchCityToCityData` module has no test file — the challengers confirmed this is a pre-existing repo-wide norm and the critical guard behavior is covered at the utility layer per the spec's own design.

The `|| {}` fallback in `MultiModalCityToCity.jsx` was challenged for creating a degenerate cache key and a potential `fetchAllTripsAction.fetch({})` call. Both concerns were weakened: `showTrips: true` only appears in the split-test data (`modeDataTest.js`), and every entry with `showTrips: true` also has fully-populated `locationData` — so `getPSearchParams` never returns `undefined` for routes that trigger the `useEffect`. The `|| {}` is also needed for `idSelector` cache-key coherence (confirmed via `wanderu-ducks-utils/src/actionDuckUtils/index.js` `id = 'default'` fallback).

### Confirmed Findings

None.

### Refuted Findings (dropped)

- **`isFetchingAllTrips` stuck `true`** — Loading state is gated by `showTrips` in the presentational component. No route in `modeData.js` or `modeDataTest.js` has `showTrips: true` with incomplete `locationData`. Not reachable.
- **Silent UTC fallback in `fetchTimeTableData`** — Intentional per spec §3.3 and §5. The inline comment correctly explains the SSR/CSR stability rationale. No `logger.warn` needed because dispatches proceed normally (no skip).
- **Misleading `|| {}` comment** — The comment is accurate: `idSelector` (`wanderu-ducks-utils/src/actionDuckUtils/index.js:144`) has `id = 'default'` as a fallback when `serializeSearchParams` returns `undefined`, silently redirecting selectors to a state slot the action never writes. The `|| {}` ensures cache-key coherence. Comment's stated reason is correct.

### Disputed Findings (held, not posted)

- **`|| {}` creates a future maintenance risk** — The `useEffect` dispatches `fetchAllTripsAction.fetch` (raw psearch action) directly. If `modeDataTest.js` ever gains a `showTrips: true` entry without `locationData`, `getPSearchParams` would return `undefined`, `psearchParams` would be `{}`, and `buildSearchBody({})` would construct an all-undefined API body. Not exploitable today (every `showTrips: true` entry in the current dataset has valid `locationData`). Worth noting for anyone extending the split-test data.
- **`fetchTimeTableData` UTC fallback branch untested** — The `i18nMoment.utc()` branch (lines 39-43) is the only new production behavior with zero test coverage. The repo has no thunk test file for this module (pre-existing norm) and the spec deliberately scoped out a thunk test file. The critical guard logic is tested at the `getPSearchParams` layer.

### Prior reviewer comment (bsaltersheridan2, 2026-05-12)

Noted: UTC vs spec §5 (nit), plan/code drift (nit), downstream broken-UI follow-up (acknowledged OOS), operational follow-ups. All valid; confirmed same observations independently.

---

*Generated by automated PR review — 2026-05-18*
