# PR Review Findings — wanderu/ui-react #868 "MAIN-5173 Multi-pax"

**Date:** 2026-04-30  
**Reviewer:** srackley (automated)  
**Classification:** NEEDS_REVIEW (zero prior reviews)  
**Review action:** REQUEST_CHANGES (1 blocking finding)

---

## PR Summary

Adds a Passengers dropdown (1–8) to the search widget behind `search-multi-pax` feature flag. Propagates `?p=N` URL param through Redux, live-check API calls, and trip summary SSR to price/reserve multiple seats. Hidden on SEO landing pages. 617 add / 205 del / 35 files / 23 commits.

**Developer checklist:** All items unchecked — no tests noted, no screenshots, no "How to Test" section filled in.

---

## Findings

### BLOCKING (1)

#### [1] `packages/wanderu-ducks-search/src/appstate/search/actions.js:138–141`
**Stale seat count persists when navigating to a non-multi-pax search (SPA)**

When `setSearchParams` is called without a `passengers` param (URL has no `?p=`), `parseInt(undefined, 10)` returns `NaN`, `NaN > 0` is `false`, so `dispatch(tripSummaryActions.setTravelerInfo({ seats: parsedPassengers }))` is **never called**. The trip summary Redux store retains whatever `seats` value was set during the previous multi-pax search.

**Concrete scenario:** User searches for 3 passengers (`?p=3`), sees multi-pax results. They click back or navigate to a different route with no `?p=` param. `setSearchParams` is invoked, stale `seats: 3` remains in the store. `selectTripPrices` in `wanderu-ducks-trips` now multiplies every listed price by 3 (the new `multipliedPricing` path). User sees 3× the real price for a single-passenger search.

**Evidence:** Challenger confirmed. `resetTravelerInfo` exists but is a dead prop (mapped in `mapDispatchToProps` but never called from the component body). No other dispatch resets `seats` to 1 during SPA navigation. `selectTravelerInfo` initial state `seats: 1` only applies at store initialization.

**Fix:** Always dispatch `setTravelerInfo` when `setSearchParams` runs, defaulting to 1 when `passengers` is absent:
```js
const parsedPassengers = parseInt(passengers, 10);
dispatch(tripSummaryActions.setTravelerInfo({ seats: parsedPassengers > 0 ? parsedPassengers : 1 }));
```
Same pattern needed in `TripSummary.tsx:325–330` for the SSR path.

---

### SUGGESTIONS (2)

#### [2] `packages/wanderu-ducks-trips/src/appstate/selectors.js:311–321`
**No tests for the `multipliedPricing` path — the core of the feature**

The new `multipliedPricing` branch (reached when `seats > 1` and no live-check pricing) has zero test coverage. `selectTripPrices` is not referenced in any test in `packages/wanderu-ducks-trips/src/appstate/tests/`. This is the pricing logic users will see for every multi-pax trip where live-check hasn't run yet — a wrong multiplication would silently show incorrect prices to the cohort under the flag.

**Evidence:** `grep -r "selectTripPrices" packages/wanderu-ducks-trips/src/appstate/tests/` returns no matches.

**Specific missing cases:**
- `seats=2`, no LC pricing: each currency value should double
- Multi-leg trip with `seats=3` and no LC: each leg multiplied independently, then summed
- `lcPricing` present with `seats > 1`: `_perSeatPricing` uses `basePricing` (per-seat) while `pricing` uses `lcPricing` (already-totaled) — no test verifies this asymmetry

#### [3] `packages/wanderu-widget/src/components/SearchWidget/PassengerSelect/` — new directory
**No tests for the new `PassengerSelect` component**

`PassengerSelect.jsx` has no test file. The component has non-trivial behavior: `handleSelectionChange` coerces the key to string before calling `onChange`, the default value is `'1'`, the plural label logic uses `value > 1`. Per CLAUDE.md: TDD is non-negotiable.

**Most important missing case:** A test asserting `value="2"` renders "2 Passengers" (plural) would also surface the string/number coercion discussed in DISPUTED finding below.

---

### DISPUTED (1 — not posted unless you approve)

*Passed verified-analysis but an independent challenger found mitigating evidence.*

#### [D1] `packages/wanderu-widget/src/components/SearchWidget/PassengerSelect/PassengerSelect.jsx:40`
**`value > 1` compares string to number implicitly**

`value` is `PropTypes.string` and `PASSENGER_OPTIONS` generates strings. `'2' > 1` works by JS coercion but is type-inconsistent. Fix: `Number(value) > 1`.

**Challenger: Weakened** — all entry points produce clean numeric strings. A non-numeric prop would be API misuse. The pattern is consistent with how `passengersInput` is typed throughout.

---

### NITS (1)

#### [N1] `packages/wanderu-widget/src/components/SearchWidget/WidgetBody/tests/__snapshots__/WidgetBody.test.jsx.snap`
**Stale snapshot from a skipped test**

`should render correctly` is `it.skip(...)` but its snapshot is still live showing `PassengerSelect`. Either delete the snapshot entry or unskip and update the test.

---

### PR DESCRIPTION

All five developer checklist items are unchecked — no "How to Test", no screenshots, tests not noted. For a feature-flagged multi-breakpoint UI change with new pricing logic, reviewers cannot validate behavior without a test plan and screenshots.

---

## Findings Summary

| # | File | Type | Finding |
|---|------|------|---------|
| 1 | `wanderu-ducks-search/…/actions.js:138` | **BLOCKING** | Stale seat count after multi-pax → single-pax navigation |
| 2 | `wanderu-ducks-trips/…/selectors.js:311` | suggestion | No tests for `multipliedPricing` path |
| 3 | `PassengerSelect/PassengerSelect.jsx` | suggestion | No tests for new component |
| D1 | `PassengerSelect/PassengerSelect.jsx:40` | DISPUTED | `value > 1` string/number coercion |
| N1 | `WidgetBody/tests/__snapshots__/` | nit | Stale snapshot from skipped test |

**Agents run:** code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer  
**Challengers run:** Finding C (Confirmed), Finding A (Weakened → DISPUTED), Finding G (Refuted)  
**False positives caught:** D (sync condition correct — mirrors origin pattern at line 282), E (seats propType already present at line 328), G (`"0"` unreachable — PASSENGER_OPTIONS starts at 1)

---

---

# PR Review Findings — wanderu/ui-react #936 "refactor(analytics): remove redundant top-level has_routed property"

**Date:** 2026-04-30
**Reviewer:** srackley (automated)
**Classification:** NEEDS_REVIEW (zero prior reviews)
**Review action:** COMMENT (suggestion only — no blocking issues)

---

## PR Summary

Removes the redundant `has_routed` top-level property from two Amplitude event emissions in `packages/wanderu-app-tripsummary`: `Checkout Started` (in `amplitudeEvents.ts`) and `trip_summary_interacted` (in `TripInfo/events.ts`). The per-trip `is_routed` field on each element of the `trips` array is unchanged. Updates 4 files: 2 source (pure deletion of `const hasRouted` + property) and 2 test files (assertion updates + regression guard added). 4 additions / 12 deletions / 4 files / 1 commit.

**Developer checklist:** Fully completed (all checkboxes ticked or marked N/A with justification). "How to Test" section filled in. PR body includes a repo-wide audit section.

---

## Findings

### BLOCKING (0)

None.

---

### SUGGESTIONS (1)

#### [1] `packages/wanderu-app-booking/src/containers/BookingForm/Steps/Payment/events.ts:101,121` (also `Confirmation.jsx:46`, `DropIn.jsx:88`)
**PR audit claim is incomplete — 4 more redundant `has_routed` emissions remain in `wanderu-app-booking`**

The PR description states: *"A repo-wide grep confirms the only redundant top-level `has_routed` emissions are on these 2 cart events."* This claim is incorrect. The `wanderu-app-booking` package has 4 more active production emission sites that still send `has_routed` as a top-level property alongside a full `trips: canonicalData?.canonicalTrips` array (which already encodes `is_routed` on every element):

| File | Line(s) | Event |
|---|---|---|
| `Payment/events.ts` | 101 | `Error Screen Viewed` |
| `Payment/events.ts` | 121 | `Processing Screen Viewed` |
| `Confirmation/Confirmation.jsx` | 46 | Confirmation `viewScreenEvent` |
| `Payment/DropIn/DropIn.jsx` | 88 | DropIn events |

All four source the same `hasRouted` from `selectCanonicalCheckoutData` (selectors.js:1068: `canonicalTrips.some(t => t.is_routed)`), and all four co-emit the full `canonicalTrips` array on the same event. The redundancy is structurally identical to the tripsummary case. Challenger independently confirmed this after reading the selector, call sites, and the `CanonicalTrip` type.

**Suggested action:** Track a follow-up PR to remove `has_routed` from these booking-package events and the `hasRouted` field from `selectCanonicalCheckoutData`'s return value. The PR description's audit section should be corrected to note these sites were intentionally left out of scope (if that was the intent) — otherwise the claim is misleading.

---

### NITS (0)

None.

---

### Positive Observations

- **Code changes are clean and correct.** Both source deletions are complete — no dangling `hasRouted` variables or stale references remain in the changed files.
- **Type integrity maintained.** Neither function's parameter type ever declared `has_routed`; the removal is purely a runtime payload change with no type updates needed.
- **Regression tests are well-designed.** Both test files add `'does not emit has_routed (#934)'` with a `is_routed: true` fixture and `expect(calledWith).not.toHaveProperty('has_routed')`. These would catch a future re-derivation attempt at the tripsummary layer.
- **No skipped or focused tests.** All tests are fully active.
- **No silent failures.** `amplitude.track` behavior is unchanged; no error-handling regressions.

---

## Findings Summary

| # | File | Type | Finding |
|---|------|------|---------|
| 1 | `wanderu-app-booking/…/Payment/events.ts:101,121` + 2 more | suggestion | Incomplete audit — 4 more `has_routed` emissions remain in booking package |

**Agents run:** code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer
**Challengers run:** Finding 1 (Confirmed — booking events co-emit `trips` and `has_routed` from same selector)
**False positives caught:** 0
