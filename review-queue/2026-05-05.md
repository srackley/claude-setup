# Review Queue — 2026-05-05

## wanderu/canopy #506 — build(deps): bump the datadog group across 1 directory with 2 updates

**Branch:** `dependabot/npm_and_yarn/datadog-26b6e0e498`  
**Classification:** NEEDS_REVIEW  
**CI:** ✅ Coverage passing, ✅ Post-deploy tests passed (dev)  

### Summary

Dependabot PR bumping `@datadog/browser-logs` and `@datadog/browser-rum` from **6.32.0 → 7.0.0** (major version bump). Only `package.json` and `pnpm-lock.yaml` changed. Five review agents confirmed no breaking API changes — all public SDK methods used remain present and compatible in v7. No type regressions. No masked tests.

### Blocking

None.

### Suggestions (1)

**S1** — `src/observability/client.ts` lines 33/95: Init catch blocks use inline ternary `error instanceof Error ? error : new Error(String(error))` instead of `toError()` from `@/lib/errors`. CLAUDE.md mandates `toError()` affirmatively; the ternary misses objects with `.message` that aren't `instanceof Error`, which Datadog SDK can throw on init failure (those become `"[object Object]"` in Datadog Logs). Fix: replace with `.withError(toError(error))` and add the import.

### Nits (2)

**N1** — `src/observability/types.ts` lines 131/139/147: JSDoc claims the Logs SDK path uses `setGlobalContextProperty()` with `'usr.'` prefix (deprecation workaround), but actual implementation calls `datadogLogs.setUser/setUserProperty/clearUser` directly. The described approach was never implemented. Update JSDocs to reflect reality.

**N2** — Awareness: In v7, `forwardErrorsToLogs: true` no longer implicitly forwards `console.error` to Datadog Logs; that now requires explicit `forwardConsoleLogs: ['error']`. No action needed given project convention prohibits raw `console.error`.

### Uncertain (requires manual check)

**U1** — Deprecation status of `datadogLogs.setUser/setUserProperty/clearUser` in v7 is unverifiable without installed node_modules. Test file has `eslint-disable-next-line @typescript-eslint/no-deprecated` on all mock accesses, implying ESLint flags them. If deprecated, production `client.ts` call sites are missing suppressions and would fail `task lint`. Run `task lint` after `task install` to resolve.

### Overall verdict

**COMMENT** (no blocking issues). Safe to merge. The `toError()` suggestion and JSDoc fix are good cleanup but not blockers for a Dependabot bump.
