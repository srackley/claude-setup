# PR Review Queue — 2026-05-13

## canopy #559 — build(deps): bump the tailwindcss group across 1 directory with 2 updates

**URL:** https://github.com/wanderu/canopy/pull/559
**Classification:** NEEDS_REVIEW
**Reviewed at:** 2026-05-13

### Context
Dependabot PR bumping `tailwindcss` and `@tailwindcss/postcss` from 4.2.4 to 4.3.0.
- Only 2 files changed: `package.json` (version pins) and `pnpm-lock.yaml` (lockfile)
- 4.3.0 is an additive minor release: new scrollbar/zoom/tab utilities, canonicalization bug fixes, no breaking changes
- Post-deploy tests passed on dev
- Coverage: 94.5% lines (target 92%), all targets met; "No changed files found" (no app code changed)
- No human review comments or threads

### Agent Findings

**code-reviewer:** No issues found — pure version bump, CI clean. Version constraint pattern (`^4.3.0`) is appropriate. Both tailwindcss packages are in sync (correct — same monorepo). prettier-plugin-tailwindcss not bumped — expected and correct.

**silent-failure-hunter:** No error handling concerns — no application code changed. All @tailwindcss/oxide platform binaries updated in lock-step in the lockfile — no version mismatches.

**type-design-analyzer:** No type concerns — no TypeScript source files changed. Project uses Tailwind v4 CSS-first configuration; no TS imports from tailwindcss or @tailwindcss/postcss exist.

**pr-test-analyzer:** No test concerns — no application code changed, CI green. One pre-existing conditional `.skip` in `e2e/ticket-lookup.spec.ts` correctly gates live-API tests on non-dev CI only; post-deploy tests ran on dev and passed.

**comment-analyzer:** No comment concerns — CI clean, no human review threads.

### Verdict
**Blocking:** none
**Suggestions:** none
**Nits:** none

Clean, safe minor version bump. Recommended action: approve.

---

## canopy #558 — build(deps): bump the amplitude group across 1 directory with 2 updates

**URL:** https://github.com/wanderu/canopy/pull/558
**Classification:** NEEDS_REVIEW
**Reviewed at:** 2026-05-13

### Context
Dependabot PR bumping two Amplitude packages:
- `@amplitude/analytics-browser`: 2.39.2 → 2.42.2 (3 minor versions)
- `@amplitude/experiment-node-server`: 1.13.5 → 1.13.6 (patch)

Only 2 files changed: `package.json` (version pins) and `pnpm-lock.yaml` (lockfile).

Notable lockfile changes:
- `analytics-core` 2.45.0 removed; replaced by 2.48.1 (analytics-browser, autocapture) and 2.48.0 (other plugins)
- New plugin added as transitive dep: `@amplitude/plugin-event-property-attribution-browser@0.2.0`
- Sub-plugins bumped: autocapture 1.25.2→1.27.1, custom-enrichment 0.1.4→0.1.8, network-capture 1.9.13→1.10.0, page-url-enrichment 0.7.5→0.7.9, page-view-tracking 2.9.6→2.11.0, web-vitals 1.1.28→1.1.32

CI: Coverage 94.5% (target 92%), all targets met; "No changed files found". Post-deploy tests passed on dev.

### Agent Findings

**code-reviewer:** Flagged (confidence 85%) that new transitive dep `@amplitude/plugin-event-property-attribution-browser@0.2.0` is pre-1.0 semver and automatically adds attribution properties to browser events. Also noted analytics-core version split (2.48.0/2.48.1) as expected pnpm behavior, no code change needed.

**silent-failure-hunter:** No new silent failure paths from the version bump. Pre-existing (not in this PR's diff): unguarded `fetchV2` in `src/features/dev-instrumentation/actions.ts:testServerAmplitudeFetchVariants` — dev-only route.

**type-design-analyzer:** No type-level concerns. All four type surfaces used (`Types.EnrichmentPlugin`, `Types.AutocaptureOptions`, `RemoteEvaluationClient`, `AmplitudeCookie`) are stable, opaque, or structurally loose relative to this bump.

**pr-test-analyzer:** No skipped/pending/focused tests in any amplitude test file. CI green, no new coverage gaps.

**comment-analyzer:** No stale or inaccurate comments. No version numbers hardcoded anywhere.

### Challenger Result

**Finding A (attribution plugin) → DISPUTED (Weakened)**

Attribution tracking was already explicitly configured in `.config/base.yaml` (`autocapture.attribution.resetSessionOnNewCampaign: true`) and wired into `amplitude.init()` at `src/services/amplitude/client.ts:44`. The new `plugin-event-property-attribution-browser@0.2.0` appears to be Amplitude extracting attribution logic from the analytics-browser core into a separate sub-package — not silently enabling a new behavior. The "silent change" framing is not supported. Residual concern: pre-1.0 semver = unstable API, but not actionable here.

### Verdict
**Blocking:** none
**Suggestions:** none
**Nits:** none
**DISPUTED:** 1 (attribution plugin semver 0.x — weakened; attribution was already intentionally active)

Clean, safe minor/patch version bump. Recommended action: approve.

---

## canopy #560 — build(deps-dev): bump the typescript-eslint group with 3 updates

**URL:** https://github.com/wanderu/canopy/pull/560
**Classification:** NEEDS_REVIEW
**Reviewed at:** 2026-05-13

### Context
Dependabot PR bumping the typescript-eslint group from 8.59.2 to 8.59.3:
- `@typescript-eslint/eslint-plugin`: 8.59.2 → 8.59.3
- `@typescript-eslint/parser`: 8.59.2 → 8.59.3
- `typescript-eslint`: 8.59.2 → 8.59.3

Only 2 files changed: `package.json` (version pins) and `pnpm-lock.yaml` (lockfile).

8.59.3 is explicitly documented as "version bump only, no code changes" — a pure alignment release within the typescript-eslint monorepo. Transitive bumps: `semver` 7.7.4→7.8.0, `brace-expansion` 5.0.5→5.0.6 (both used within the typescript-eslint chain, no application code impact). TypeScript peerDependency constraint `>=4.8.4 <6.1.0` is unchanged and continues to be satisfied by the project's `typescript@6.0.3`.

### Agent Findings

**code-reviewer:** No issues. All 3 packages correctly synchronized at 8.59.3 in both `package.json` and `pnpm-lock.yaml`. `^8.59.3` version constraints are appropriate. TypeScript compat confirmed. Pre-existing: `eslint-config-next@16.2.4` resolves its own `typescript-eslint@8.59.2` peer separately — benign pnpm deduplication behavior, not introduced by this PR.

**silent-failure-hunter:** No concerns. ESLint config uses `strictTypeChecked` + `stylisticTypeChecked`; no problematic disable blocks. Auto-merge workflow correctly gates on CI (lint + test) passing before merging. One pre-existing gap (no `--max-warnings 0` flag on `pnpm lint`) is not introduced by this PR.

**type-design-analyzer:** No type concerns. PeerDependency range identical between 8.59.2 and 8.59.3; `typescript@6.0.3` satisfies `<6.1.0`. Zero imports of `@typescript-eslint/*` in TypeScript source files — all usage is ESLint-config-time only.

**pr-test-analyzer:** No test concerns. Single `test.skip` in `e2e/ticket-lookup.spec.ts:45` is an intentional environment guard — unrelated to ESLint. No `.only` or other focus-scope markers in any source test files.

**comment-analyzer:** Clean. No version numbers hardcoded in comments outside `package.json`. All `eslint-disable` inline comments reference stable rule names only.

### Verdict
**Blocking:** none
**Suggestions:** none
**Nits:** none

Clean, safe patch-level version bump. Recommended action: approve.
