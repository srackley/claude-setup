# Review Queue — 2026-04-28

---

## wanderu/ui-react #933 — fix add return trip button on trip summary

**URL:** https://github.com/wanderu/ui-react/pull/933  
**Classification:** NEEDS_REVIEW  
**Review action:** COMMENT (no blocking findings)

### Findings

**[suggestion 1] hooks.ts duplicates hooks from parent file**

`packages/wanderu-app-tripsummary/src/components/TripSummary/TripSelectionSummary/TripInfo/ReturnButton/hooks.ts`

The PR adds `useArriveTripSelector` and `useDepartTripSelector` — both are exact duplicates of `useTripArriveDataSelector` and `useTripDepartDataSelector` already exported from `TripSelectionSummary/hooks.ts` (same selectors, same `useSelector` call, no equality comparator, identical return types). Fix: delete `ReturnButton/hooks.ts` and import from `../../hooks` instead.

**[suggestion 2] No test for `dispatch(searchActions.setDirection('return'))`**

`packages/wanderu-app-tripsummary/src/components/TripSummary/TripSelectionSummary/TripInfo/ReturnButton/tests/ReturnButton.test.tsx`

The dispatch is the stated fix for the stale-redirect bug, but the date-click test only asserts the URL shape. If the dispatch line were removed, all tests would still pass. `searchReducerMap` is already injected into the store — adding `expect(store.getState()[...].direction).toBe('return')` needs no scaffolding.

### Context

The root fix (replacing `useLocation`/URL-param parsing with Redux trip selectors and dispatching `setDirection('return')` before redirect) is correct. Snapshot confirms URL changed from `/return/undefined/undefined/...` to `/return/Orlando%2C%20FL/Miami%2C%20FL/...`. Both suggestions are non-blocking improvements.

---

## wanderu/ui-react #931 — feat(tripsummary): render carrierCombinedFee as plural "Carrier Fees"

**URL:** https://github.com/wanderu/ui-react/pull/931  
**Classification:** NEEDS_REVIEW  
**Review action:** COMMENT (no blocking findings)

### Findings

**[suggestion 1] `label` field is untyped — filter at line 95 is unguarded**

`packages/wanderu-app-tripsummary/src/components/TripSummary/TripSelectionSummary/utils.ts:11–18, 95`

`formatFees` assigns `label` via a `let label` + if/else block. TypeScript widens the three string literals to `string` rather than inferring the literal union. The filter on line 95 (`fee.label === 'carrier-fee' || fee.label === 'carrier-fees'`) is therefore an unguarded string comparison: renaming `'carrier-fees'` inside `formatFees` (or the filter) compiles with zero TypeScript errors while silently dropping all combined-fee rows from the rendered list.

Fix: `let label: 'platform-fee' | 'carrier-fee' | 'carrier-fees';` — or extract a named `FeeLabel` type. Optionally co-locate a `CARRIER_FEE_LABELS` constant so the filter reads `CARRIER_FEE_LABELS.includes(fee.label)` instead of a duplicated OR-clause.

**[suggestion 2] No test covers `carrierCombinedFee` + individual carrier fee coexistence**

`packages/wanderu-app-tripsummary/src/components/TripSummary/TripSelectionSummary/tests/utils.test.ts`

`formatPrices` iterates `Object.values(fees)` unconditionally and adds every fee to the sum used to compute `totalPrice`. If an API response ever contains both `carrierCombinedFee` and individual carrier fee rows simultaneously (e.g. during a pservs rollout window), `totalPrice` would be overstated with no error. No test covers this coexistence scenario or asserts on `totalPrice` under it.

**[nit 1] Load-bearing OR-filter has no comment**

`packages/wanderu-app-tripsummary/src/components/TripSummary/TripSelectionSummary/utils.ts:95`

The `|| fee.label === 'carrier-fees'` clause is load-bearing — removing it silently drops all `carrierCombinedFee` rows from the rendered list. A short comment explains the coupling between `formatFees`'s two carrier-fee labels and this filter, preventing a future maintainer from treating the OR-condition as duplication.

```ts
// Both labels required: 'carrier-fee' (per-fee rows) and 'carrier-fees' (carrierCombinedFee).
// Dropping either silently removes those rows from the rendered fee list.
```

### Context

The root change is correct: `carrierCombinedFee` → `'carrier-fees'` label mapping and the complementary filter update prevent silent fee drops. All 12 locale `.po` files include the `carrier-fees` translation key. 2 suggestions, 1 nit — no blocking findings.

---

## wanderu/canopy #474 — build(deps): bump the opentelemetry group with 4 updates

**URL:** https://github.com/wanderu/canopy/pull/474  
**Classification:** NEEDS_REVIEW  
**Review action:** COMMENT (no blocking findings)

### Breaking Change Safety Assessment

Bumps `@opentelemetry/api-logs`/`instrumentation`/`sdk-logs` from 0.214.0 → 0.215.0 and `sdk-trace-base` from 2.6.1 → 2.7.0.

The 0.215.0 release has two breaking changes:
1. `LogRecordExporter.forceFlush()` now required — **No impact**: canopy has no custom `LogRecordExporter` implementations; the interface lives entirely inside `@vercel/otel`.
2. `Logger#enabled()` added to OTel's `Logger` interface — **No impact**: canopy's `Logger` type is a local interface with no relationship to OTel's `Logger`; `LoggerWrapper` wraps LogLayer.

`sdk-trace-base` 2.7.0 has no breaking changes. Sampler signatures (`AlwaysOnSampler`, `ParentBasedSampler`, `TraceIdRatioBasedSampler`) are identical. No TypeScript compatibility issues. No test gaps. No skipped or focused tests.

**Bump is safe to merge.**

### Findings

**[suggestion 1] Use `toError()` instead of inline error guard**

`src/observability/tracing.ts:42`

The catch block uses:
```ts
.withError(error instanceof Error ? error : new Error(String(error)))
```

CLAUDE.md requires using `toError(error)` from `@/lib/errors` in catch blocks. This is more than a style issue: `toError()` preserves `.message` from non-`instanceof Error` objects (e.g. `{ message: "Service already registered", code: "DUPLICATE" }`). The inline guard produces `"[object Object]"` for such objects, losing the actual error message from `registerOTel` in the startup-failure log.

Fix: replace line 42 with `toError(error)` and add `import { toError } from '@/lib/errors'`.

**[suggestion 2] `"OpenTelemetry tracing initialized"` fires even when init failed**

`src/instrumentation.ts:58-62`

After `initializeTracing(...)`, line 59 checks `config.opentelemetry.tracing.enabled` — a static config flag — not whether `registerOTel` actually succeeded. If `registerOTel` throws, `initializeTracing` catches it, logs an error, and returns `void`. The outer code then unconditionally logs "OpenTelemetry tracing initialized". Startup logs would contain a failure message followed by a success message — actively misleading.

Fix: return `boolean` from `initializeTracing` and gate the success log on the return value.

### Context

Both suggestions are pre-existing issues not introduced by this bump. The bump itself is safe.
