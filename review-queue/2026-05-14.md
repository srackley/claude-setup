# Review Queue — 2026-05-14

## canopy#448 — [PLAT-447] Add issue-writing conventions to planning guidance

**URL:** https://github.com/wanderu/canopy/pull/448
**Author:** meag-wanderu
**Classification:** NEEDS_REVIEW
**Review action:** COMMENT

### Summary

Adds 11 lines to `.claude/rules/planning.md`: a "Writing issues" section with guidance on what epic bodies should contain and four anti-patterns to avoid. Documentation-only PR; CI passing.

**What it does:** Gives teams explicit guidance not to duplicate sub-issue status in parent epic bodies.
**Why:** Issue #447 — maintaining status tables and sub-issue summaries in parent issues creates drift and dual-maintenance burden.

---

### SUGGESTIONS (1)

**`.claude/rules/planning.md` — "Writing issues" section**

The PR's own acceptance criteria (from issue #447) require guidance on three things: parent issues, sub-issues, and anti-patterns. The PR delivers parent issue guidance and anti-patterns, but contains no positive guidance on what belongs in a *sub-issue* body. The challenger confirmed: no sub-issue body guidance exists anywhere in `.claude/rules/` — not in `planning.md`, not in any other rules file. A developer writing a sub-issue for the first time has no guidance here on what to include (acceptance criteria, link to parent, scope boundary, definition of done, spec pointers).

**Suggested addition:** A `### Sub-issue bodies` subsection (or brief paragraph) specifying what goes in a sub-issue: the unit of work it accomplishes, acceptance criteria, a link to the parent epic, relevant spec sections, and any dependencies on other sub-issues.

Note: The section heading `## Writing issues` implies broader coverage than the current content delivers; this resolves once sub-issue guidance is added.

**Evidence:** Challenger grepped all files in `.claude/rules/` — zero results for sub-issue body guidance outside the anti-patterns list.

---

### DISPUTED (2 — not surfaced as comments)

**Model epic #270 not explicitly updated:** Challenger weakened — the new "Writing issues" section immediately follows the model epic reference and functions as the supplement the acceptance criteria called for. Fixing GitHub issue #270 itself is out of scope for a repo doc PR.

**"automatically" overstates GitHub project board:** Challenger weakened — GitHub's sub-issue list (distinct from project board automation) always tracks sub-issue status natively without configuration. The claim is accurate for that mechanism.

---

### DROPPED (challenger-refuted)

- Structural discontinuity between sections: pre-existing in base branch; headings are sufficiently distinct.
- "within seconds" informal: consistent with document register; surrounding context makes intent clear.

---

### Agents run

code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer
Challengers: A confirmed, B weakened, D weakened, E refuted, F refuted

---

## ui-react#964 — chore: align Claude Code setup with Wanderu conventions

**URL:** https://github.com/wanderu/ui-react/pull/964
**Author:** noahwelch
**Classification:** NEEDS_REVIEW
**Review action:** COMMENT

### Summary

Renames `docs/` → `.docs/`, adds index READMEs for progressive disclosure, rewrites CLAUDE.md from ~200 to ~90 lines (verbose content replaced with links to `.docs/`), adds knowledge docs for webpack/redux, updates `.gitignore` to track `.claude/` artifacts and `.docs/plans/`. No production code changed.

7 existing discussion threads from scottpcipriano (all opinion-level).

---

### SUGGESTIONS (2)

**1. `.docs/README.md:10` — Plans described as "mostly gitignored" contradicts reality**

The line reads: _"Mostly gitignored: authored locally during implementation."_

But `.docs/plans/README.md` line 5 says the exact opposite: _"Plans are tracked in git by default."_ The `.gitignore` has zero entries for plans, and all 8 files under `.docs/plans/` are confirmed tracked in git. A developer or agent reading `.docs/README.md` first would incorrectly treat plans as ephemeral/local-only.

**Suggested fix:** Update line 10 to: _"[plans/](plans/): milestone-based implementation plans paired with specs. Tracked in git by default."_

**Evidence:** Challenger grepped `.gitignore` (97 lines) — no "plan" or ".docs" entries. All plan files confirmed tracked.

---

**2. `.docs/knowledge/README.md:20` — Domains list omits `build/` and `redux/`**

The Domains section lists only `analytics`. This PR adds two new knowledge subdirectories — `build/` and `redux/` — each with their own README, but neither appears in the index. A reader following the progressive-disclosure chain through `.docs/README.md` → `.docs/knowledge/README.md` hits a dead end at `analytics` only.

**Suggested addition:**
```
- [build](./build/README.md) — Webpack configuration, bundling constraints, and upgrade blockers.
- [redux](./redux/README.md) — Redux state debt, selector memoization, and debugging guidance.
```

**Evidence:** Challenger confirmed 3 subdirectories exist (`analytics/`, `build/`, `redux/`); only `analytics` listed at line 20.

---

### NITS (2)

**3. `.docs/plans/2026-05-11-amplitude-bot-detection-implementation-plan.md:5` — Hardcoded local path as link display text**

Line 5 display text is `/Users/noahwelch/Projects/Wanderu/ui-react/.docs/specs/...` — the href is correct. Fix: use `[2026-05-11-amplitude-bot-detection-865.md](../specs/2026-05-11-amplitude-bot-detection-865.md)`.

**4. `.docs/plans/2026-05-11-amplitude-bot-detection-implementation-plan.md:118` — Stale SESSION-NOTES.md reference**

Line 118: _"This repo's CLAUDE.md references a `SESSION-NOTES.md`..."_ — this PR removes that reference from CLAUDE.md. After landing, any agent reading the plan gets incorrect info. Fix: update to reflect that CLAUDE.md no longer references SESSION-NOTES.md.

---

### DISPUTED (1 — not surfaced as comment)

**`.claude/settings.json`** — Missing trailing newline. Technically correct but ~49% of the repo's JSON files also lack trailing newlines with no enforced convention. Not a meaningful violation in this project's context.

---

### Agents run

code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer
Challengers: A confirmed, B confirmed, C confirmed, D weakened, E confirmed

---

## ui-react#879 — chore(deps): bump brace-expansion from 1.1.12 to 1.1.14 in /packages/wanderu-flag-icons

**URL:** https://github.com/wanderu/ui-react/pull/879
**Author:** dependabot[bot]
**Classification:** NEEDS_REVIEW
**Review action:** APPROVE

### Summary

Pure lockfile-only security patch in `packages/wanderu-flag-icons/yarn.lock`. Updates `brace-expansion` from 1.1.12 to 1.1.14, fixing ReDoS vulnerabilities GHSA-7h2j-956f-4vf2 and GHSA-f886-m6hf-6m8v.

**What it does:** Updates a single transitive dependency (`brace-expansion@^1.1.7`) in the isolated Yarn v1 lockfile for the `wanderu-flag-icons` sub-package. 1 file changed, 4 lines.
**Why:** Security patch — brace-expansion 1.1.x had ReDoS vulnerabilities; 1.1.14 is the patched version. No API changes, no runtime behavior changes.

---

### BLOCKING (0)

None.

---

### SUGGESTIONS (0)

None.

---

### NITS (0)

None.

---

### Additional notes

- Root yarn.lock (Yarn Berry v6) independently resolves brace-expansion to 1.1.13; this is expected and not a conflict in a mixed Yarn v1/Berry monorepo.
- `brace-expansion` is a transitive dependency of `minimatch` used only at build time (flag icon CSS generation); no production bundle impact.
- Pre-existing: `FlagIcon.test.jsx` uses Enzyme (legacy pattern per CLAUDE.md); unrelated to this PR.

---

### Agents run

code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer
No challengers needed (zero VALID findings).
