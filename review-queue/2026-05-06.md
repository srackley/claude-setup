# Review Queue — 2026-05-06

## wanderu/ui-react #879

**PR:** [chore(deps): bump brace-expansion from 1.1.12 to 1.1.14 in /packages/wanderu-flag-icons](https://github.com/wanderu/ui-react/pull/879)
**Classification:** NEEDS_REVIEW
**Reviewed:** 2026-05-06

### Summary

Dependabot security patch bumping `brace-expansion` 1.1.12 → 1.1.14 in `packages/wanderu-flag-icons/yarn.lock` only (2 lines changed). Patches GHSA-7h2j-956f-4vf2 and GHSA-f886-m6hf-6m8v (ReDoS vulnerabilities). `brace-expansion` is a transitive build/tooling dependency only — not part of the runtime bundle.

### Agent Findings

**code-reviewer:** One finding below threshold (55% confidence) — registry URL inconsistency (`registry.yarnpkg.com` vs internal `npm.wanderu.com/dev`). Classified FALSE POSITIVE via verified-analysis: pre-existing from prior accepted Dependabot PRs, not introduced by this PR.

**silent-failure-hunter:** No issues. Pure security patch, no behavioral changes to well-formed inputs, no runtime impact.

**type-design-analyzer:** No issues. No type changes.

**pr-test-analyzer:** No issues. No skipped/focused tests. No new tests needed.

**comment-analyzer:** No issues. No code comments changed.

### Verdict

**Blocking:** none  
**Suggestions:** none  
**Nits:** none  

APPROVE. Clean security patch — all 5 review agents found no actionable issues.

---

## wanderu/ui-react #929

**PR:** [build(deps-dev): bump postcss from 8.5.8 to 8.5.10](https://github.com/wanderu/ui-react/pull/929)
**Classification:** NEEDS_REVIEW
**Reviewed:** 2026-05-06

### Summary

Dependabot security patch bumping `postcss` 8.5.8 → 8.5.10 in `package.json` and `yarn.lock`. Fixes XSS via unescaped `</style>` in non-bundler cases. `postcss` is a `devDependency` used at build time only (via `postcss-loader` + `autoprefixer`/`cssnano`) — not part of the runtime bundle.

### Agent Findings

**code-reviewer:** No issues. Version bump is mechanically correct; `yarn.lock` correctly maintains two stanzas (`^8.5.3` for stylelint at 8.5.3, `^8.5.10` for main app at 8.5.10) — expected Yarn Berry behavior, not a regression.

**silent-failure-hunter:** No issues. Pure build-time tool; no runtime fallback paths exist. Build failures from postcss surface loudly (webpack exits non-zero). No `resolutions` override that could keep the vulnerable version.

**type-design-analyzer:** No issues. No TypeScript changes.

**pr-test-analyzer:** No issues. No skipped/focused tests introduced. No new tests needed for a dev-dependency security patch.

**comment-analyzer:** No issues. No code comments changed.

### Verdict

**Blocking:** none  
**Suggestions:** none  
**Nits:** none  

APPROVE. Clean security patch — all 5 review agents found no actionable issues.

---

## wanderu/canopy #506

**PR:** [build(deps): bump the datadog group across 1 directory with 2 updates](https://github.com/wanderu/canopy/pull/506)
**Classification:** NEEDS_REVIEW
**Reviewed:** 2026-05-06

### Summary

Dependabot major-version bump of `@datadog/browser-logs` and `@datadog/browser-rum` from 6.32.0 to 7.0.0. Two files changed: `package.json` and `pnpm-lock.yaml`. No application code modified. Lock file is mechanically correct — specifiers, resolved versions, and peer deps are all consistent.

Five review agents found no functional breakage. All named imports used in the project (`DefaultPrivacyLevel`, `TraceContextInjection`, `RumInternalContext`, `StatusType`) remain exported and structurally identical in v7. All `RumInitConfiguration` and `LogsInitConfiguration` options used by the project are present in v7. No removed options are used in this codebase.

Key v7 changes verified as safe for this codebase:
- `defaultPrivacyLevel` default changed (`mask` → `mask-user-input`): non-issue — project explicitly sets `mask-user-input` in `.config/base.yaml`
- `propagateTraceBaggage` now defaults to `true`: safe — `allowedTracingUrls` is same-origin only, no CORS impact
- `forwardErrorsToLogs` no longer captures `console.error`: non-issue — CLAUDE.md explicitly prohibits `console.error` in production code; only occurrence is in dev-only `src/components/dev/`
- Removed init options (`allowFallbackToLocalStorage`, `usePciIntake`, `betaTrackActionsInShadowDom`, etc.): not used anywhere in the codebase

### Agent Findings

**code-reviewer:** Three findings; two reclassified after verification (deprecated-API premise was wrong per v7 `.d.ts`; console.error concern refuted). One finding on `forwardErrorsToLogs` behavioral change dropped — project prohibits raw `console.*` in production.

**silent-failure-hunter:** Five findings; two converge with code-reviewer. Additional: `as never` cast at `client.ts:161` (confirmed nit), post-init SDK calls unguarded (UNCERTAIN — no concrete v7 throwing evidence), `startView` session_renewal note (informational only).

**type-design-analyzer:** `RumDefaultPrivacyLevelSchema` missing `mask-unless-allowlisted` (weakened — gap is inert, project hardcodes `mask-user-input`). `as never` cast confirmed. Critical nuance: confirmed the non-deprecated `setUser(User & { id: string })` overload is what the code calls — the JSDoc "(deprecated)" notes are the real issue.

**pr-test-analyzer:** `forwardErrorsToLogs` not explicitly asserted in init test (weakened — Zod schema validates it as required; test mock already includes it). No skipped/focused tests found.

**comment-analyzer:** Three JSDoc blocks in `types.ts` (lines 128–149) claim `setGlobalContextProperty('usr.*')` but code calls direct user API; "(deprecated)" annotations stale in v7. Confirmed nit.

**docs-researcher (verified-analysis):** All three library-behavior claims verified from `@datadog/browser-logs@7.0.0` and `@datadog/browser-rum@7.0.0` `.d.ts` files:
- `forwardErrorsToLogs` change: Verified — `console.error` now requires `forwardConsoleLogs: ['error']`
- `DefaultPrivacyLevel` has 4 values including `mask-unless-allowlisted`: Verified
- `setUserProperty` and `clearUser` NOT deprecated in v7; only id-less `setUser` overload is: Verified

### Verdict

**Blocking:** none  
**Nits:** 2

1. **Stale JSDoc in `types.ts` lines 128–149** (`setUser`, `setUserProperty`, `clearUser`): JSDoc claims Logs SDK uses `setGlobalContextProperty('usr.*')` and marks direct user methods as deprecated. Both claims are wrong — code calls the direct user API (`client.ts:137/188/197`), and in v7 `setUserProperty`/`clearUser` have no `@deprecated` tag. JSDoc describes an implementation that was never built.

2. **`as never` cast at `client.ts:161` missing explanatory comment**: Cast is necessary (bridges `Record<string, unknown>` public API to SDK's constrained `Context` type) but undocumented. Per `.claude/rules/api-clients.md` convention, such casts require a comment. A brief inline explanation would help future maintainers distinguish intentional workaround from lazy cast.

**Disputed (not posted):**
- `eslint-disable-next-line @typescript-eslint/no-deprecated` suppressions on `setUser` mock accesses — challenger found the suppression may be warranted due to overload resolution on the mock type
- `RumDefaultPrivacyLevelSchema` missing `'mask-unless-allowlisted'` — gap is real but inert; project hardcodes `mask-user-input` in all environments
- `forwardErrorsToLogs: true` not explicitly asserted in init test — substantially mitigated by Zod schema validation at config boundary
