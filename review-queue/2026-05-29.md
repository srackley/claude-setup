# PR Review Queue — 2026-05-29

## [ui-react#987] fix(footer): remove Flights link from TRAVEL section
https://github.com/wanderu/ui-react/pull/987

**Classification:** NEEDS_REVIEW  
**Author:** jtstults-w  
**Updated:** 2026-05-28  
**Existing reviews:** fyeung86 APPROVED ("No findings")

### Summary
Deletion-only PR. Removes `FLIGHT_TICKETS` constant and its entry from `TRAVEL_LINKS` in `wanderu-component-footer/src/constants.js`. Updates two snapshot files. `.po` translation files intentionally left with orphaned `footer-flight` key (managed by third-party provider).

### Review Agents Run
1. **code-reviewer** — No findings. Clean removal; no remaining references to `FLIGHT_TICKETS` or `/flight-tickets/` in any `.js`/`.jsx`/`.ts`/`.tsx` source file. Constant was never exported, so no risk of breakage elsewhere in the monorepo.
2. **silent-failure-hunter** — No findings. Pure static configuration; no error handling, no async code, no fallback logic affected.
3. **type-design-analyzer** — No findings. Plain JavaScript file; no type definitions added or modified.
4. **pr-test-analyzer** — No significant findings. Snapshots are the correct test vehicle for a static data removal. No `.skip`/`.only`/`.todo` patterns. Pre-existing note: Footer snapshot uses a router-link mock that renders all links as `<A [object Object] />` (URL-blind) — detects count changes only. LinkTable snapshot is higher-fidelity and clearly shows `/flight-tickets/` removed. Pre-existing tech debt, not introduced by this PR.
5. **comment-analyzer** — No findings. File has no comments.

### Findings
None. PR is clean and correct.

**Blocking:** none  
**Suggestions:** 0  
**Nits:** 0

---

## [canopy#597] build(deps-dev): bump the commitlint group across 1 directory with 2 updates
https://github.com/wanderu/canopy/pull/597

**Classification:** NEEDS_REVIEW  
**Author:** dependabot[bot]  
**Updated:** 2026-05-26  
**Existing reviews:** none

### Summary
Dependabot PR bumping `@commitlint/cli` and `@commitlint/config-conventional` from 20.5.3 to 21.0.1 (major version bump v20→v21). The v21.0.0 breaking change (minimum Node.js v22) is not an issue — canopy uses Node 24.3.0 per `.tool-versions`. Only `package.json` and `pnpm-lock.yaml` changed. CI passed: coverage report shows no changed files (expected), post-deploy tests passed on dev. Branch is behind main (mergeable_state: behind — dependabot is rebasing).

### Review Agents Run
1. **code-reviewer** — One finding: `commitlint.config.mjs` lines 16–17 cite `lodash.mergewith` as the mechanism used by `@commitlint/resolve-extends`, but v21 uses `es-toolkit` instead. Also: "Verified at @commitlint/load@20.5.0" is stale. Lock file confirms v20 packages (`@commitlint/load@20.2.0`, etc.) remain as optional transitive deps of `cz-conventional-changelog` — expected and benign.
2. **silent-failure-hunter** — No findings. Husky `commit-msg` hook invokes commitlint with no error suppression. Pre-existing note: commitlint is local-only (no CI enforcement), unrelated to this PR.
3. **type-design-analyzer** — No findings. Only `package.json` and `pnpm-lock.yaml` changed.
4. **pr-test-analyzer** — No findings. `@commitlint` is a dev dependency with no presence in the test suite. CI results are appropriate.
5. **comment-analyzer** — One finding: `commitlint.config.mjs` lines 5, 16–17, and 19–20 contain three stale claims: version `@9.3.0` (installed is 9.3.1 — pre-existing), `lodash.mergewith` attribution (es-toolkit since at least v20.5.x), and `Verified at @commitlint/load@20.5.0` (active version is 21.0.1).

### Findings

**[commitlint.config.mjs:16–17, 19–20] VALID nit — Stale comment: `lodash.mergewith` attribution and version pin are both wrong**

The workaround comment explaining why a full `parserOpts` object is provided states: _"@commitlint/resolve-extends uses lodash.mergewith, which lets the user object win when types mismatch. Verified at @commitlint/load@20.5.0."_

The challenger independently confirmed:
- `@commitlint/resolve-extends` has used `es-toolkit/compat`'s `mergeWith` since at least v20.5.x (not just v21) — the lodash attribution was already wrong before this PR.
- "Verified at @commitlint/load@20.5.0" is stale — active version is 21.0.1.
- Additionally, under `es-toolkit/compat` merge semantics, a partial `parserOpts` override (only `issuePrefixes: []`) might actually work correctly today, meaning the workaround could be unnecessary — but this has not been re-verified.
- No test or CI guard covers commitlint behavior, so the workaround's necessity is unconfirmed for either v20.5.x or v21.

The comment should note `es-toolkit/compat` as the merge implementation and update the version annotation. Ideally, a maintainer should verify whether the partial-override approach now works before the full-object approach is continued indefinitely.

Draft comment:
> `commitlint.config.mjs` line 16–17: The comment attributes the type-mismatch merge behavior to `lodash.mergewith`, but `@commitlint/resolve-extends` has used `es-toolkit/compat`'s `mergeWith` since v20.5.x (confirmed in lock file — `@commitlint/resolve-extends@21.0.1` → `es-toolkit: 1.46.1`; no lodash dependency). The "Verified at @commitlint/load@20.5.0" note is also stale — active version is 21.0.1. Worth updating the comment to name `es-toolkit` and to re-verify whether the workaround is still necessary under current merge semantics (partial `parserOpts` with only `issuePrefixes: []` may now work correctly).

**Blocking:** none  
**Suggestions:** 0  
**Nits:** 1
