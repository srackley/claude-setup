# Review Queue — 2026-05-21

## wanderu/ui-react#896 — fix: per-env Amplitude cookieDomain via cookieOptions.domain (#873)

**Reviewed:** 2026-05-21
**Classification:** NEEDS_REVIEW
**Branch:** feat/amplitude-cookie-domain-873 @ 081d9ed1

### Summary

Fixes Amplitude SDK cookie domain scoping so `AMP_*` cookies land on the shared parent domain (`.wanderu.com` / `.wanderu.dev`) rather than the app hostname. Adds `cookieOptions: { domain: appSettings?.config?.amplitude?.cookieDomain }` to `amplitude.init()` and adds per-env `cookieDomain` to `.config/dev.yaml` + `.config/prod.yaml`. Also bumps `@amplitude/analytics-browser` `^2.22.0 → ^2.38.1` (resolves to 2.42.3).

### VALID — Suggestion

**packages/wanderu-analytics/src/tests/AmplitudeProvider.test.tsx:862–871 — Test only verifies key presence, not value threading**

The new test uses:
```ts
expect(initOptions).toHaveProperty('cookieOptions');
expect(initOptions.cookieOptions).toHaveProperty('domain');
```

Jest's `toHaveProperty('domain')` passes even when `domain` is `undefined` (key present, value absent). The test environment config has no `cookieDomain` field, so `domain` is `undefined` in every assertion. The test would pass for a typo in the config path, wrong object reference, or hardcoded `undefined`. The PR's entire purpose is to wire `cookieDomain` from per-env config through to `amplitude.init()` — the test doesn't prove that wiring exists.

**Challenger verdict:** Confirmed.

**Recommended fix:** Add a second `it` block using the `setConfig` mock helper to inject a known `cookieDomain` value and assert it reaches `amplitude.init`:
```ts
it('threads the configured cookieDomain value to cookieOptions.domain', async () => {
  setConfig({ ...defaultConfig, config: { ...defaultConfig.config, amplitude: { ...defaultConfig.config.amplitude, cookieDomain: '.wanderu.com' } } });
  // render + flushEffects ...
  expect(initOptions.cookieOptions?.domain).toBe('.wanderu.com');
});
```

### UNCERTAIN (not posted without explicit approval)

**packages/wanderu-analytics/src/AmplitudeProvider.tsx:201–203 — `cookieOptions: { domain: undefined }` vs. omitting `cookieOptions` entirely**

Two review agents disagreed on SDK behavior for when `cookieDomain` is absent from config. If the SDK uses `'domain' in options` semantics (rather than a null-guard), passing `{ domain: undefined }` could silently degrade cookie scoping. A safer implementation: `...(cookieDomain && { cookieOptions: { domain: cookieDomain } })` — omits the key entirely when unconfigured.

### Refuted / FALSE POSITIVE Findings

- **pilot.yaml missing cookieDomain**: FALSE POSITIVE — pilot.yaml has no amplitude block (no `apiKey`), SDK never initializes per `if (!apiKey) return` guard. PR description documents this correctly.
- **Comment "legacy top-level `cookieDomain`"**: REFUTED — refers to Amplitude SDK v1.x→v2.x API change, not a codebase migration. Accurate and informative.

### Counts
- Blocking: 0
- Suggestions: 1
- Nits: 0
- Uncertain: 1

---

## wanderu/canopy#590 — 🤖 Dispatcher: Rename `app_source` → `source` on Ticket Lookup events

**Reviewed:** 2026-05-21
**Classification:** NEEDS_REVIEW
**Branch:** dispatcher/584-rename-app-source-source-on-ti @ f603df2c2a92f280a2383ad18d8aabc3fa871ed2

### Summary

Renames the Amplitude enrichment plugin so the emitted event property key changes from `app_source` to `source` — the key the Amplitude data dictionary and downstream Snowflake queries actually expect. Renames the plugin file, exported function, and internal plugin name throughout. Updates `client.ts` import and call site. Adds a spec and plan doc. Post-deploy CI passed on dev. Coverage at 94.58% (above 92% threshold).

### VALID — Suggestion

**`.docs/specs/rename-app-source-source-on-ti/spec.md:2` — `status: READY` should be `IMPLEMENTED`**

The spec is added in this same PR with `status: READY`, but the PR also delivers the complete implementation — all five acceptance criteria are satisfied by the renames in this PR. The planning convention says small features go `READY → IMPLEMENTED` in the same PR as the implementation ("Every PR that changes behavior covered by a spec must update the spec in the same PR"). After merge, the spec will show `status: READY` while the feature is already shipped, which inverts the source-of-truth hierarchy (READY = "not yet real"). The fix is a one-line change: `status: IMPLEMENTED`.

**Challenger verdict:** Confirmed.

### DISPUTED (not posted without explicit approval)

**`src/services/amplitude/source-plugin.test.ts:16–19` — Object keys not alphabetized in assertion**

Current: `{ source: 'canopy', existing: 'value' }`. The code-style rule says alphabetize object keys, which would put `existing` before `source`.

Challenger: **Weakened** — The rule applies with no test-file exclusion, but the current order mirrors the implementation's spread: `{ ...event.event_properties, source: appSource }` places existing properties first and appends `source`. The test assertion puts `source` first, which is actually the reverse of the spread order. Either way there is zero functional difference (Vitest's `toEqual` is order-agnostic). The current order has a mild readability justification (emphasizing the injected key), but the rule exists and a trivially safe fix is available.

**`src/services/amplitude/client.test.ts:79,84` — Stale variable names `appSourceCall` / `appSourceAddOrder`**

The test description was updated to `'registers source-enrichment plugin before amplitude.init'` but the two internal variables still use the old `appSource` prefix. The analogous Snowplow block uses `snowplowCall` etc.

Challenger: **Weakened** — No practical confusion: both variables are initialized on the very next lines, and `appSourceCall`'s `findIndex` expression explicitly searches for `plugin.name === 'source-enrichment'`. A reader can follow without ambiguity. The names are stale but not misleading in context. The rename would be cosmetically cleaner.

### Refuted / FALSE POSITIVE Findings

- **Stale parameter name `appSource` in `createSourcePlugin`**: REFUTED — `appSource` is semantically accurate (the application's source identifier). Renaming to `source` would produce `source: source` at the usage site (`source: appSource` on line 8), which is less readable.

### Counts
- Blocking: 0
- Suggestions: 1
- Nits: 0
- Disputed: 2
