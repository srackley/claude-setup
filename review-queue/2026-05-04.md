# PR Review Queue — 2026-05-04

## wanderu/canopy#494

**Title:** build(deps): bump the amplitude group across 1 directory with 4 updates
**URL:** https://github.com/wanderu/canopy/pull/494
**Author:** dependabot[bot]
**Classification:** NEEDS_REVIEW
**Branch:** dependabot/npm_and_yarn/amplitude-3366eddc29

### Summary

Dependabot PR bumping 4 Amplitude SDK packages:
- `@amplitude/analytics-browser`: 2.38.1 → 2.42.0
- `@amplitude/analytics-node`: 1.5.52 → 1.5.57
- `@amplitude/experiment-js-client`: 1.20.4 → 1.21.1
- `@amplitude/experiment-node-server`: 1.13.4 → 1.13.5

New transitive dep introduced: `@amplitude/plugin-event-property-attribution-browser@0.2.0` (auto-bundled into analytics-browser 2.42.0).

Changed files: 2 (package.json, pnpm-lock.yaml). No source code modified.

CI: Tests pass (87.75% line coverage > 68% target). Post-deploy tests passed on dev.

### Agent Findings

**code-reviewer:** No issues ≥80 confidence. All 4 bumps stay within existing `^` semver ranges (minor/patch only). New transitive dep not imported directly; CSP unaffected.

**silent-failure-hunter:** No issues in PR diff. Production Amplitude error handling is sound across all 4 call sites. Pre-existing gap noted in `src/features/dev-instrumentation/actions.ts` (dev-only, not in scope).

**type-design-analyzer:** No type design issues. Amplitude types used conservatively — no structural narrowing, no fragile casts over SDK internals.

**pr-test-analyzer:** Test coverage adequate. All 4 upgraded packages exercised by existing tests in `src/services/amplitude/`. No skipped or focused tests. CI green.

**comment-analyzer:** No comment issues. No version-specific references in source comments.

### Verdict

**Blocking:** none
**Suggestions:** none
**Nits:** none

Clean, low-risk dependency bump. All CI passes. Recommend approval.

---

## wanderu/ui-react#935

**Title:** feat(tripsummary): split OurBus fees into distinctly-named line items (#932)
**URL:** https://github.com/wanderu/ui-react/pull/935
**Author:** bsaltersheridan2
**Classification:** NEEDS_REVIEW
**Branch:** feat/ourbus-fee-names-932
**Head SHA:** 15458917ecc9ff3051154c6dd1547773abe99c6f

### Summary

Splits OurBus fees (`usourBookingFee`, `usourFacilityFee`) into distinctly-named line items in the Trip Summary. Previously both displayed as "Carrier Fee". The fix adds `labelKeys: string[]` to fee objects and calls `t(fee.labelKeys, { defaultValue: t('carrier-fee') })` to resolve the first matching translation key or fall back to the generic label. Also renames PO translation keys from `ourbus-booking-fee` → `usourBookingFee` and `ourbus-facility-fee` → `usourFacilityFee` across 12 locale files to match the API fee IDs.

Closes #932. Replaces closed ui-react#931 and pservs#538/542.

Existing review: noahwelch's 4 suggestions (use `t()` for defaultValues) are all resolved and incorporated. One unresolved comment from eway-wanderu ("We really want to hard code these in the ui?") is outdated. fyeung86's unresolved comment about `carrier-fee` msgctxt answered below.

### Agent Findings

**code-reviewer:** (95%) hr-hr.po not migrated to new key names — all other 12 locales updated. (85%) `label: string` declared in both FeesProps interfaces but never read by either component. (82%) Component tests use `toHaveBeenCalledWith` (call signatures) rather than `screen.getByText` (rendered output); violates CLAUDE.md "test behavior not implementation".

**silent-failure-hunter:** Confirmed hr-hr.po issue — missingKeyHandler is a no-op so Croatian users silently see generic "Carrier Fee" with no log/error. `defaultValue: t('carrier-fee')` is unreachable dead code for carrier fees (labelKeys already ends in 'carrier-fee').

**type-design-analyzer:** `label` is redundant alongside `labelKeys` — both encode same binary classification; drift risk. `labelKeys: string[]` too loose — actual domain is `['platform-fee'] | [string, 'carrier-fee']`; tuple type would make i18next fallback intent explicit. `FeesProps` duplicated inline in both component files.

**pr-test-analyzer:** No `.skip`/`.only`/`.todo` markers — clean. Confirms component tests check call signatures not rendered behavior. Empty fees input / non-USD currency not tested in utils.test.ts.

**comment-analyzer:** fyeung86's question: `carrier-fee` does not need `msgctxt` — it is only a generic last-resort fallback; adding context would fragment the PO entry. Platform-fee `defaultValue: t('platform-fee')` also unreachable. PR's "Out of scope" claim for `TripLeg.tsx` in `wanderu-app-booking` is accurate.

### Challenger Verdicts

| # | Finding | Verdict |
|---|---------|---------|
| 1 | hr-hr.po not migrated | **Confirmed** — `usourBookingFee`/`usourFacilityFee` absent from hr-hr.po; no i18next aliasing mechanism; Croatian users get generic Croatian "Carrier Fee" silently |
| 2 | `label` unused in FeesProps | **Confirmed** — never read in render code; safe to remove from interfaces |
| 3 | Component tests implementation-focused | **Weakened** — `t(labelKeys, ...)` call signature IS the i18next contract; `getByText` still preferable per CLAUDE.md but not "purely implementation" |
| 4 | `defaultValue` unreachable | **Confirmed** — verified against i18next 19.0.1 source; `'carrier-fee'` always intercepts before `defaultValue` |
| 5 | `labelKeys: string[]` too loose | **Confirmed** — TS 5.8 supports tuples; i18next TFunction accepts `readonly string[]` (tuple is valid subtype); all test fixtures already match exact tuple shapes |

### Verdict

**Blocking (1):**
- `packages/wanderu-shellapp/src/translations/baseApp/hr-hr.po` — OurBus keys not migrated. Fix: rename `msgid "ourbus-booking-fee"` → `msgid "usourBookingFee"` and `msgid "ourbus-facility-fee"` → `msgid "usourFacilityFee"` (same strings, just the keys). One-line-per-key fix.

**Suggestions (1):**
- `labelKeys` should be typed as `['platform-fee'] | [string, 'carrier-fee']` instead of `string[]` — tuple makes i18next fallback intent explicit; no call-site breakage, no test fixture changes needed

**Nits (2):**
- `label: string` unused in both FeesProps interfaces — components only use `fee.id` and `fee.labelKeys`
- `defaultValue: t('carrier-fee')` unreachable for carrier fees (and `defaultValue: t('platform-fee')` unreachable for platform fees) — consider removing or adding a brief "defense-in-depth" comment

**Disputed (1 — not posted):**
- Component tests assert on `t()` call signatures rather than `screen.getByText`; weakened by challenger (the array call signature IS the i18next contract), but `screen.getByText` additions would better align with CLAUDE.md

**Unresolved reviewer question (fyeung86):** No `msgctxt` needed on `carrier-fee` — it's an intentional generic fallback, not a contextually distinct string.
