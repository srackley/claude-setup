# PR Review Queue — 2026-05-26

---

## wanderu/ui-react #896 — fix: per-env Amplitude cookieDomain via cookieOptions.domain (#873)

https://github.com/wanderu/ui-react/pull/896

**Classification:** NEEDS_REVIEW
**Review action:** COMMENT (no blocking findings)

### Summary

Fixes Amplitude cookie scoping so `AMP_*` cookies are set on the shared parent domain (`.wanderu.com` prod / `.wanderu.dev` dev) instead of the app's serving hostname, preserving device ID continuity across ui-react ↔ canopy ↔ nexus. Implementation in `AmplitudeProvider.tsx` is correct. Pilot/base are correctly excluded (no apiKey → Amplitude never initialized). Main gap is the test.

### Blocking

none

### Suggestions (1)

**packages/wanderu-analytics/src/tests/AmplitudeProvider.test.tsx:869–870** — Weak test: only asserts `cookieOptions` and `domain` keys exist, never asserts the actual value. The test config has no `cookieDomain`, so `domain` is always `undefined` — both `toHaveProperty` assertions pass trivially. A refactor that broke the config-to-SDK wiring would not be caught. Fix: call `setConfig` to inject a known `cookieDomain`, then assert `expect(initOptions.cookieOptions?.domain).toBe('.wanderu.com')`.

### Nits (1)

**packages/wanderu-analytics/src/tests/AmplitudeProvider.test.tsx:865–868** — Comment claims the test verifies per-env sourcing ("it sources from `appSettings.config.amplitude.cookieDomain`") but the test never exercises a non-undefined value. Remove or qualify that second claim.

### Dropped Findings

- **pilot.yaml missing cookieDomain** — FALSE POSITIVE. `AmplitudeProvider.tsx:169` has an `!apiKey` guard that exits before init; pilot has no amplitude block so Amplitude is never initialized. PR author explicitly noted this.
- **wanderu-config untyped** — PRE-EXISTING. `wanderu-config` is a JS module with no TypeScript declarations. Not introduced by this PR.

---

## wanderu/ui-react #980 — chore(deps): bump @tootallnate/once from 1.1.2 to 2.0.1

https://github.com/wanderu/ui-react/pull/980

**Classification:** NEEDS_REVIEW
**Review action:** COMMENT (no blocking findings)

### Summary

Dependabot patch bump of the `@tootallnate/once@npm:2` resolution in yarn.lock from 2.0.0 to 2.0.1. The fix in 2.0.1 resolves a promise hang when an `AbortSignal` is aborted before the `once()` listener is attached. This package is a transitive dependency of `http-proxy-agent` (used by `make-fetch-happen` in the npm toolchain and `jsdom` in the Jest test environment). It is not a direct application dependency and no application source code references it.

### Blocking

none

### Suggestions

none

### Nits

none

### Agent Findings Summary

All 5 review agents (code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer) found no issues meeting the ≥80 confidence threshold.

**Notable pre-existing issue (not from this PR):** `liveCheckCachedTrip` accepts an `AbortSignal` parameter but does not forward it to the `WapiService.get` HTTP call. Pre-existing, no regression from this PR.

### Verdict

Safe to merge. Routine patch-release lockfile update with a correctness fix in npm tooling internals. No tests needed, no API changes, no type impacts.

---

## wanderu/canopy #597 — build(deps-dev): bump the commitlint group across 1 directory with 2 updates

https://github.com/wanderu/canopy/pull/597

**Classification:** NEEDS_REVIEW
**Review action:** COMMENT (no blocking findings)

### Summary

Dependabot major version bump: `@commitlint/cli` and `@commitlint/config-conventional` from 20.5.3 → 21.0.1. Changed files: `package.json` and `pnpm-lock.yaml` only. Breaking changes in v21.0.0: (1) minimum Node >=22.12.0 — canopy uses Node 24.3.0, safe; (2) new CLI output format with `--legacy-output` transition flag — no impact since no script parses commitlint stdout. CI passed (tests + post-deploy dev green).

### Blocking

none

### Suggestions

none

### Nits (1)

**commitlint.config.mjs:5–34** — The comment block's technical rationale is stale after this v21 bump. Three specific inaccuracies in one block (one follow-up PR covers all):

1. **"Verified at @commitlint/load@20.5.0"** (line ~19): The installed version is now 21.0.1. The stated version was never even the installed version (the prior lockfile had 20.5.3). Should be updated to `@commitlint/load@21.0.1`.

2. **"lodash.mergewith lets user object win when types mismatch"** (lines ~14–18): This claim is factually false for v21.0.1. The bug it describes (commitlint/commitlint#4640 — partial `parserPreset` overrides losing `headerPattern`) was fixed by pre-resolving extended `parserPreset` strings before the merge. The fix is in `@commitlint/resolve-extends@21.0.1` and confirmed in source: *"Previously this was skipped when the user provided any parserPreset, which caused partial user overrides (e.g. just issuePrefixes) to lose the extended preset's headerPattern (see #4640)."* Also, `lodash.mergewith` was replaced with `es-toolkit`'s `mergeWith` as of ~20.5.2. The full-object copy is still safe and correct, but the stated reason for it is wrong. Comment should be updated to say the full object is kept as a defensive stability measure, and note that a simpler `parserPreset: { parserOpts: { issuePrefixes: [] } }` would now work correctly.

3. **"conventional-changelog-conventionalcommits@9.3.0"** (line ~5): Installed version is 9.3.1. Content is identical between the two patch versions, but the re-diff instruction points at the wrong source version.

**Bonus gap in the re-diff instruction:** The comment says "When bumping conventional-changelog-conventionalcommits, re-diff…" — it should also say "When bumping @commitlint to a new major version, re-evaluate whether the full parserOpts copy is still necessary."

### Agent Findings Summary

All 5 review agents (code-reviewer, silent-failure-hunter, type-design-analyzer, pr-test-analyzer, comment-analyzer) reviewed the PR. No findings above the 80% confidence threshold for blocking issues. The only findings were the comment accuracy issues above, all independently confirmed by challengers.

**Notable non-finding:** `@commitlint/load@20.2.0` coexists in lockfile alongside 21.0.1 — expected, driven by `cz-conventional-changelog@3.3.0`'s optional peer dependency. No conflict under pnpm.

### Verdict

Safe to merge. No blocking issues. `commitlint.config.mjs` comment needs a follow-up update but is not a merge blocker — the code is correct and CI is green.
