# PR Review Queue — 2026-06-01

## [canopy#601] build(deps-dev): bump the development-deps group across 1 directory with 6 updates

**URL:** https://github.com/wanderu/canopy/pull/601  
**Classification:** NEEDS_REVIEW  
**Updated:** 2026-05-26  

### Summary

Dependabot PR bumping 6 devDependencies:

| Package | From | To |
|---|---|---|
| @hey-api/openapi-ts | 0.96.1 | 0.97.2 |
| @types/node | 25.6.0 | 25.9.1 |
| eslint-plugin-storybook | 10.3.5 | 10.4.0 |
| jsdom | 29.0.2 | 29.1.1 |
| msw | 2.13.4 | 2.14.6 |
| react-hook-form | 7.73.1 | 7.76.0 |

Changes: 2 files (package.json + pnpm-lock.yaml). CI: all coverage thresholds passing.

### Verdict: Clean to approve

**Blocking:** none  
**Suggestions:** none  
**Nits:** none  

### UNCERTAIN (heads-up, not a blocker)

**@hey-api/openapi-ts 0.97.x — possible runtimeConfigPath behavior change**

Agent flagged that 0.97.0 may have changed `runtimeConfigPath` resolution so paths starting with `../` are resolved from CWD rather than passed verbatim. Both `openapi-ts.wapi.config.ts` and `openapi-ts.wtix.config.ts` use `../../lib/*-client-config` paths commented as "relative to output dir." If the behavior changed, the next run of `task generate-api-wapi` or `task generate-api-wtix` would produce broken import paths in the regenerated client files.

Cannot verify without changelog access (not installed). Currently committed generated files are unaffected — breakage would only surface on the next manual codegen run. Recommend checking the 0.97.x changelog before running codegen.

### Refuted findings (not posted)

- **react-hook-form devDep misclassification**: `useForm` is a runtime import in `TicketLookupForm.tsx:4`, but Dockerfile uses `pnpm install -r --offline` without `--prod` and production uses Next.js standalone output. No effect on deployed artifact.

---

## [canopy#597] build(deps-dev): bump the commitlint group across 1 directory with 2 updates

**URL:** https://github.com/wanderu/canopy/pull/597  
**Classification:** NEEDS_REVIEW  
**Updated:** 2026-05-26  

### Summary

Dependabot PR bumping @commitlint/cli and @commitlint/config-conventional from v20.5.3 to v21.0.1. Major version bump. Changes: 2 files (package.json + pnpm-lock.yaml). CI: all coverage thresholds passing, post-deploy tests passing.

Key v21 changes: Node ≥22.12.0 required (project uses Node 24 ✓), yargs 17→18, dropped lodash.mergewith and import-meta-resolve from internal deps, conventional-changelog-conventionalcommits unchanged at 9.3.1.

### Verdict: Clean to approve

**Blocking:** none  
**Suggestions:** none  
**Nits:** 1

### Nit: Stale comments in `commitlint.config.mjs` (not blocking)

The "Why the full object" comment block (lines 14–20) has two stale references after this bump:

1. `@commitlint/resolve-extends uses lodash.mergewith` — v21 replaced lodash.mergewith with es-toolkit/compat. lodash.mergewith is no longer in the dependency tree.
2. `Verified at @commitlint/load@20.5.0` — project now runs @commitlint/load@21.0.1.

The workaround code itself still works correctly. Suggested replacement for lines 16–20:

```
// @commitlint/resolve-extends uses es-toolkit/compat's mergeWith (replaced
// lodash.mergewith in v21), which lets the user object win when types mismatch.
// A partial override (issuePrefixes: [] only) therefore omits breakingHeaderPattern
// and headerPattern, causing subject-empty/type-empty errors on `feat!:` syntax.
// Verified at @commitlint/load@21.0.1. The full object ensures all patterns are present.
```

### Items cleared

- Node.js engine: Node 24 satisfies v21's ≥22.12.0 requirement ✓
- yargs@18 / yargs-parser@22 / cliui@9: require ^20.19.0 || ^22.12.0 || ≥23 — Node 24 ✓
- conventional-changelog-conventionalcommits unchanged at 9.3.1, no re-diff needed ✓
- No project TypeScript files import from @commitlint/* ✓
- No test gaps: pure dep bump, CI coverage passing ✓

---

## [canopy#595] build(deps): bump postcss from 8.5.14 to 8.5.15 in the tailwindcss group

**URL:** https://github.com/wanderu/canopy/pull/595  
**Classification:** NEEDS_REVIEW  
**Updated:** 2026-05-26  

### Summary

Dependabot PR bumping postcss from 8.5.14 to 8.5.15 (patch-level, tailwindcss group).

| Package | From | To |
|---|---|---|
| postcss | 8.5.14 | 8.5.15 |

Changes: 2 files (package.json + pnpm-lock.yaml). The 8.5.15 release is a performance fix only: "Fixed declaration parsing performance." CI: all coverage thresholds passing, no changed files in test coverage.

Lockfile update is structurally correct: new resolution block and snapshot with the expected dependency triple (nanoid 3.3.12, picocolors 1.1.1, source-map-js 1.2.1) matching prior versions. Both postcss@8.5.14 and postcss@8.5.15 coexist in the lockfile because @tailwindcss/postcss@4.3.0 peers against 8.5.14; this is correct pnpm behavior and will resolve once @tailwindcss/postcss is updated.

### Verdict: Clean to approve

**Blocking:** none  
**Suggestions:** none  
**Nits:** none  
