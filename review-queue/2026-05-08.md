# PR Review Queue — 2026-05-08

---

## wanderu/ui-react#953 — Fix/tripsummary depart return buttons

**Author:** noahwelch  
**Branch:** fix/tripsummary-depart-return-buttons  
**Classification:** NEEDS_REVIEW  
**Reviewed:** 2026-05-08

### Summary

Solid fix for two real production bugs: (1) passenger count not persisting when returning to search from Trip Summary (#949), and (2) the "change return trip" button incorrectly navigating to depart results and clearing the return date. Applies the same `dispatch(searchActions.setDirection(direction))` pattern established in PR #933 for the add-return-trip button.

The implementation is sound. `buildSearchUrl`'s `passengers: seats` addition correctly handles `undefined` seats (qs.stringify omits the param cleanly), and the `departDateTimestamp`/`returnDateTimestamp` removal from the `getSearchBannerState` call is safe — they were excess positional arguments silently ignored by JS. Tests cover the core behavior well.

Prior review by bsaltersheridan2 has two open findings (mobile back-arrow stale-direction bug, clamped seats question) — both remain open and valid.

---

### VALID Findings

**Blocking:** none  
**Suggestions:** none  
**Nits: 1**

**N1: `onNavigate` not listed in `BannerCardList.propTypes` `complete` shape**  
`packages/wanderu-components-search/src/components/SearchBanner/BannerCardList/BannerCardList.jsx`

`BannerCard.propTypes` was updated with `onNavigate: PropTypes.func` (good), but `BannerCardList.propTypes`'s shape for `complete` array items was not. The `complete` shape uses `PropTypes.arrayOf(PropTypes.shape({...}))` — a declared contract — and `onNavigate` is destructured from each card and spread onto `BannerCard`, so it belongs in the shape. Fix: add `onNavigate: PropTypes.func` to the `complete` shape definition at line ~22.

---

### DISPUTED Findings

**D1: BannerCardList click handler calling `onNavigate` has no direct test**  
No test simulates a click on a completed-step `BannerCard` to verify `onNavigate?.()` fires. Challenger weakened: the key invariants are covered in `SearchBanner.test.jsx` (onNavigate wiring) and `TripSummary.test.jsx` (dispatch chain). The untested seam is a one-liner glue call; practical regression risk is low.

**D2: `direction` parameter is `any` across JSX boundary**  
`onChangeTripClick` receives `direction` as implicit `any` because `SearchBanner` is `.jsx`. Challenger weakened: `SearchBanner` hardcodes exactly two call sites using correct constants; no `Direction` type exists in the codebase; adding one here would be isolated/inconsistent in this mixed JS/TS maintenance-mode repo.

---

### Review Action

**COMMENT** — nit only, no blocking issues. Safe to merge after addressing N1 (trivial PropTypes addition) and the two open bsaltersheridan2 findings.

---

## wanderu/ui-react#950 — (feature) Re-land amplitude source enrichment plugin without ??=

**Author:** Tendol  
**Branch:** feat/amplitude-source-plugin-v2  
**Classification:** NEEDS_REVIEW  
**Reviewed:** 2026-05-08

### Summary

Solid re-land. The original `??=` crash was correctly diagnosed (webpack 4 / acorn can't parse logical-assignment without the babel plugin), and the fix uses the same explicit `if (!event_properties)` guard pattern already established by `SnowplowIdsPlugin.execute()` in the same file. The factory-vs-class pattern choice is well-suited to the parameterized design. Registration ordering is well-tested using `mock.invocationCallOrder`. Unit test coverage for `execute()` behavior is thorough (5 cases including the "preserve caller-set source" invariant).

bsaltersheridan2 has 3 open threads (unresolved at review time):
1. `=== undefined` guard silently passes `source: null` — should either widen to `== null` or add a test pinning the strict-undefined behavior
2. Suggested test for whichever semantics the author picks on the null case
3. Stylistic note: a comment explaining factory-vs-class would help future maintainers

These are all valid and still open.

---

### VALID Findings

**Blocking:** none  
**Suggestions:** none  
**Nits:** none

### DISPUTED Findings (weakened by independent challengers — not posted)

**D1: Integration test verifies registration but not that execute() is invoked**  
`packages/wanderu-analytics/src/tests/AmplitudeProvider.test.tsx`

The provider integration test confirms the plugin is passed to `amplitude.add` with correct `name` and `type`, and that it precedes `amplitude.init`. It does not verify that `execute()` is ever called by the Amplitude SDK pipeline.  
Challenger weakened: `createSourcePlugin.test.ts` exhaustively tests `execute()` as a pure function (5 cases). The remaining gap is whether the Amplitude SDK invokes `execute()` for registered enrichment plugins, which is an SDK-internal invariant and not testable with the mock setup. The wiring evidence (correct name, type, and ordering) is adequate for a maintenance-mode codebase.

**D2: Comment uses "autocapture" as if it were an event type**  
`packages/wanderu-analytics/src/AmplitudeProvider.tsx:137` — informal shorthand that reads coherently in context; the `autocapture` config block is immediately visible below the comment. Not a real maintainability risk.

### REFUTED Findings (dropped)

**R1: Plugin registered without deduplication guard**  
Amplitude SDK (`analytics-core@2.20.0`) deduplicates plugins by name in `Timeline.register()`. React StrictMode double-invocations are safely handled by the SDK itself.

---

### Review Action

**COMMENT** — no blocking issues. Clean re-land. Three open bsaltersheridan2 threads remain unresolved (null guard semantics + test, factory comment). Safe to merge after those are addressed.

---

## wanderu/ui-react#879 — chore(deps): bump brace-expansion from 1.1.12 to 1.1.14 in /packages/wanderu-flag-icons

**Author:** dependabot[bot]  
**Branch:** dependabot/npm_and_yarn/packages/wanderu-flag-icons/brace-expansion-1.1.13  
**Classification:** NEEDS_REVIEW  
**Reviewed:** 2026-05-08

### Summary

Security patch for two ReDoS CVEs (GHSA-f886-m6hf-6m8v, GHSA-7h2j-956f-4vf2) in the `brace-expansion` package, scoped to `packages/wanderu-flag-icons/yarn.lock` only. The change is 4 lines in a Yarn 1 lockfile — version string and resolved URL for `brace-expansion` bumped from 1.1.12 to 1.1.14. No source code, type definitions, or test files modified.

brace-expansion is a build-time transitive dependency pulled in by `minimatch` in the flag-icons package build tooling. It does not ship to runtime. The dependency graph (balanced-match, concat-map) is unchanged between versions. CI (test + build-and-deploy) passes green.

One pre-existing note: the `brace-expansion` entry uses the public `registry.yarnpkg.com` resolver rather than the private `npm.wanderu.com` mirror used by most other entries. This is pre-existing (5 packages in this lockfile already used the public registry before this PR) and not introduced by this change.

---

### VALID Findings

**Blocking:** none  
**Suggestions:** none  
**Nits:** none

---

### Review Action

**COMMENT** — no findings from any of the 5 review agents. Safe to merge.
